# mandatory_rest

## Testing User Flow

1. Install dependencies:
   `npm install`

2. Start the local server in one terminal:
   `npm run start`

3. Open a second terminal and run the test flow script:
   `./test-flow.sh`

4. Watch the server output for the database operations and the cron algorithm executing the Ephemeral Data flow.
